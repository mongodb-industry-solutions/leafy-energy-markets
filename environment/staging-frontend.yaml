env:
  NODE_ENV: staging
  AWS_REGION: us-east-1
  PORT: "8080"
  # Internal cluster URL for server-side API calls (next.config.js rewrites)
  INTERNAL_API_URL: http://leafy-energy-markets-backend:8080

serviceAccount:
  enabled: true
  irsa:
    accountId: "275662791714"
    roleName: "kanopy-staging-cicd-irsa"
